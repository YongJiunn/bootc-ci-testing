# Set global options
data_dir: "/var/lib/vector"

# Vector's API (disabled by default)
# Enable and try it out with the `vector top` command
# NOTE: this is _enabled_ for helm chart deployments, see: https://github.com/vectordotdev/helm-charts/blob/develop/charts/vector/examples/datadog-values.yaml#L78-L81
api:
  enabled: false
# address = "127.0.0.1:8686"

# Ingest data by tailing one or more files
sources:
  journald:
    type: journald
  postgres:
    type: file
    include:
      - /var/lib/pgsql/POSTGRESQL_MAJOR_VERSION/data/log/*.log
  
# transforms: # if needed

# Send structured data to a short-term storage
sinks:
  loki:
    encoding.codec: logfmt # not sure if this is correct
    type: loki
    inputs:
      - journald
      - postgres
    endpoint: http://localhost:3100 # how to templatize this?